# GodotFestival 项目开发约定（AGENTS）

本文件是项目级开发流程约定，所有开发 Agent 都必须遵守。全局 skill 不能替代本文件。

## 核心原则

1. 不做半成品：每次只实现一个小模块，验证通过后再做下一个。
2. 不一次性写完所有功能，严格增量开发。
3. 每个操作或任务完成后，必须向用户详细汇报：做了什么、改了哪些文件、验证结果、下一步。
4. 遇到不确定的 Godot API、编辑器操作或机制时，先查官方文档再回答或实现，禁止凭印象猜测。
5. 测试采用明确授权制：除非用户在当次请求中明确要求测试，否则不得启动项目、运行场景、模拟输入、截取运行截图或进行玩法验证；只做避免改坏工程的静态检查。

## 增量开发顺序

2026-08 项目重构：玩法从横版过关改为纵向跳跃攀登（跳跳乐），旧机关与多管理器方案已废弃，旧文档归档于 `docs/archive/`。

当前全局单例：

- EventManager：全局信号总线（`scripts/managers/event_manager.gd`）
- SaveManager：存档读取、新游戏清除与退出保存（`scripts/managers/save_manager.gd`）
- SettingsManager：设置、全局音频与 BGM 场景切换（`scripts/ui/settings_manager.gd`）

其他模块按需生长，不提前搭空壳。开发顺序按 `docs/开发路线图_v2.md` 的里程碑执行：
玩家手感 → 纵向相机与最小世界 → 跳跃交互（逐个验证）→ 死亡重生与 HUD → 后续按需扩展。

## 每个小模块的完成步骤

0. 操作编辑器场景前，先用 gdmcp CLI 检查当前场景的 `is_modified` 状态；有未保存改动时先保存或与用户确认，不直接外部改写文件或强制重载。
1. 实现该模块的最小可用版本，不提前扩展。
2. 用 gdmcp CLI 做最小静态检查；运行、试玩和效果验证默认交给用户，只有用户明确要求时才执行。
3. 向用户汇报：做了什么、改了哪些文件、验证结果、下一步。

## 技术约束

- Godot 4.7.1 + GDScript，Compatibility 渲染器。
- 地形构建优先 TileMapLayer；独立交互物（平台、弹跳垫等）用独立场景，参数配置化。地图关卡设计由用户构思。
- 工具优先级：默认只使用 gdmcp CLI（沙箱内可用，覆盖 MCP 全部能力，节省 token），完整用法见 `docs/开发工具_gdmcp_CLI指南.md`。
- 只有 gdmcp CLI 真的无法完成某个任务时，才向用户确认；用户同意后才能使用完整 MCP 工具目录。
- Godot 官方 console CLI 与 GUI 可执行文件一律禁用，不直接启动 Godot 引擎进程。
- 沙箱内禁止直接启动 Godot 引擎进程（GUI 或 console 版）。
- 代码与文档全部使用 UTF-8。
- 所有 `.gd` 脚本必须带中文注释；至少说明脚本职责、每个非平凡函数的作用，以及关键状态/时序逻辑。
- 脚本中需要策划/关卡人员调节的参数才使用 `@export`；每个 `@export` 上方用中文 `##` 说明用途、方向和单位，作为 Inspector 提示。运行逻辑内部常量不要导出。

## 文档与数值

- 策划、数值、路线图放在 `docs/`；旧《滞变》横版过关文档在 `docs/archive/`，仅作历史参考。
- 当前执行版路线图为 `docs/开发路线图_v2.md`。
- 数值定稿后以 Excel 表为准；代码与表不一致时，先改表再改代码。

## Git 分支约定

- `main`：稳定版，验证通过才合入。
- `dev`：日常开发线。
- `feature/prototype`：试验性功能线。
- 先推 `dev`，验证通过后再合入 `main`。
- 不主动推送代码到远程仓库，除非用户明确要求推送。
- 推送前必须向用户展示本次改动差异（diff），用户确认后才能推送。
